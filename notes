To-Do
-- route params .

demo:
-- avoid traversing through wrong methods
-- express route overriding // if route with same path already there, over-write it
-- iterator or generator for running handlers // take a look at generators
