To-Do
-- route params .
-- Edit README.md



demo:
-- iterator or generator for running handlers // take a look at generators

Doubts:
-- What more details to include while sending response object
-- what are events in nodejs? Do they change some sort of global state of nodejs or so?

Ideas:
-- config.js to store static data like http default headers, status messages, etc.
-- utils file to store repetetive chunk of code .
-- Create a server to do a CRUD .
-- Be creative .
-- Using node events module to change state or so.
-- Use hash maps to create reusable data .
-- Modularize .